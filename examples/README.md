# Example fixtures

These small files exercise the four-stage CSV pipeline documented in the main
README and tested in `tests/`:

- `clean_names/`: raw and cleaned name tables
- `merge_supp_data/`: title and nickname inputs
- `preprocess/`: pattern-generation inputs
- `search/`: names and a text corpus

They are test and import/export fixtures, not package runtime data.
